N.B. Copied and migrated directory structure and import paths from /usr/local/go/src/* of a go v1.10 installation.

Necessary workaround for using the `go list` stuff without being allowed to import go's /internal/ packages.

Under this path they are exposted as "external".

